<?php

use App\Models\User;

return [

    /*
    |--------------------------------------------------------------------------
    | 認証の既定値
    |--------------------------------------------------------------------------
    |
    | 標準で使用する認証ガードとパスワードリセット設定を指定する。
    |
    */

    'defaults' => [
        'guard' => env('AUTH_GUARD', 'web'),
        'passwords' => env('AUTH_PASSWORD_BROKER', 'users'),
    ],

    /*
    |--------------------------------------------------------------------------
    | 認証ガード
    |--------------------------------------------------------------------------
    |
    | セッションなど、利用者を認証する方式とユーザープロバイダを指定する。対応ドライバはsession。
    |
    */

    'guards' => [
        'web' => [
            'driver' => 'session',
            'provider' => 'users',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | ユーザープロバイダ
    |--------------------------------------------------------------------------
    |
    | 認証対象の利用者を取得する方法とモデルを指定する。対応ドライバはdatabase、eloquent。
    |
    */

    'providers' => [
        'users' => [
            'driver' => 'eloquent',
            'model' => env('AUTH_MODEL', User::class),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | パスワードリセット
    |--------------------------------------------------------------------------
    |
    | リセットトークンの保存先、有効時間、再発行間隔を指定する。
    |
    */

    'passwords' => [
        'users' => [
            'provider' => 'users',
            'table' => env('AUTH_PASSWORD_RESET_TOKEN_TABLE', 'password_reset_tokens'),
            'expire' => 60,
            'throttle' => 60,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | パスワード再確認の有効時間
    |--------------------------------------------------------------------------
    |
    | パスワード再確認後に再入力を求めるまでの秒数を指定する。
    |
    */

    'password_timeout' => env('AUTH_PASSWORD_TIMEOUT', 10800),

];
